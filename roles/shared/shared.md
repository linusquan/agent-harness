### Project Context

This repository serves as the orchestrator for a multi-agent coding system. It contains a collection of curated prompts and scripts designed to facilitate seamless coordination between AI agents. The core codebase being managed and developed by these agents is located within the src directory.

### Special Branch config

- local environment has no docker-stack.yaml on main branch, but the deploy-test and deploy-prod branch has it. Remote server uses git worktree loading them 